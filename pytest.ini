[pytest]
testpaths = tests
# La raíz del repo debe estar en sys.path para que los tests importen `main`, `bot` y `src`.
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:src.*
